//! The composer: the input box, the what-is-it-doing line above it
//! (`✻ thinking… 12s`), and the suggestion row. Implemented in the
//! tui-render step.

/// Draw the composer for one frame.
pub fn draw(_frame: &mut ratatui::Frame) -> anyhow::Result<()> {
    anyhow::bail!("not implemented yet: composer view")
}
